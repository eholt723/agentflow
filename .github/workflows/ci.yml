name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r api/requirements.txt
          pip install "pytest>=8.0" "pytest-asyncio>=0.23" "httpx>=0.27"

      - name: Run unit tests
        run: pytest tests/ -m "not integration" -v

  deploy:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [test]

    steps:
      - uses: actions/checkout@v4

      - name: Push to Hugging Face Spaces (orphan)
        run: |
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions"
          git remote add space https://eholt723:${{ secrets.HF_TOKEN }}@huggingface.co/spaces/eholt723/AgentFlow
          git checkout --orphan hf-deploy
          git add -A
          git commit -m "AgentFlow — Hugging Face Space deployment"
          git push space hf-deploy:main --force
